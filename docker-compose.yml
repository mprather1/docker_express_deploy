version: '2'

services: 
  express:
    build: .
    image: mprather1/docker_express:0.1.0
    container_name: shintech-express
    env_file:
      - ./docker.env
    ports:
      - 8000:8000
    restart: always
    volumes:
      - ./data:/data